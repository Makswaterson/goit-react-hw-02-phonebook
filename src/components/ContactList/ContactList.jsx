import PropTypes from 'prop-types';

export const ContactList = ({ }) => {
    return (
        <ul>
            {contacts.map(contact)=> {
                return (
            
                );
        }}
    </ul>
    )
}